apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: agent-manager
    app.kubernetes.io/name: agent
    app.kubernetes.io/managed-by: kustomize
  name: agent-manager-metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: agent-manager
